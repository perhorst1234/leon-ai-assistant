#!/usr/bin/env bash
set -euo pipefail
repo_root=$(cd "$(dirname "$0")/.." && pwd -P)
python="$repo_root/.venv/bin/python"
[ -x "$python" ] || { printf '%s\n' 'Run scripts/leon-setup first.' >&2; exit 69; }
exec "$python" -m leon_control_plane.aliexpress_mcp "$@"
