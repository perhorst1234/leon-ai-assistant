[project]
name = "leon-ai-assistant"
version = "0.1.0"
description = "Local-first control plane for the Leon/Gaia personal AI assistant."
requires-python = ">=3.12"
dependencies = []

[project.scripts]
leon-dashboard = "leon_control_plane.server:main"

[tool.pytest.ini_options]
testpaths = ["tests"]

